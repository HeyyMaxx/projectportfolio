# Staging note

This directory is **temporary**. Phase 0 of the tracker was built here only so the work is
committed and safe — the plan calls for a dedicated `calisthenics-tracker` repository, and this
session's GitHub app lacks permission to create one (`403 Resource not accessible by integration`).

Once `HeyyMaxx/calisthenics-tracker` exists, this whole directory moves there and is deleted from
`projectportfolio`.
