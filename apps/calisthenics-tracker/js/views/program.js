export const label = 'Program';
export function view() {
  return `<div class="empty">
    <h2>No program yet</h2>
    <p>Back, Push, Core and Legs — with the shared warmup and skill blocks — get seeded in Phase 1.</p>
  </div>`;
}
